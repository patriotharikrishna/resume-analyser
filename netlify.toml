[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Environment variables - add these in Netlify UI instead of here for security
# HF_TOKEN = "your_hugging_face_api_token_here"
